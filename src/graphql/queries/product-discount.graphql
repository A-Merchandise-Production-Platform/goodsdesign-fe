query GetAllDiscountByProductId($productId: String!) {
  getAllDiscountByProductId(productId: $productId) {
    createdAt
    discountPercent
    id
    isActive
    isDeleted
    minQuantity
    name
    updatedAt
  }
}
